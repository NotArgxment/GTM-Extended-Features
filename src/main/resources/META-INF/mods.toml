modLoader = "javafml"
loaderVersion = "[47,)"
license = "${mod_license}"
issueTrackerURL = "https://github.com/NotArgxment/GTM-Extended-Features/issues"

[[mods]]
    modId = "${mod_id}"
    version = "${version}"
    displayName = "${mod_name}"
    displayURL = "${mod_url}"
    logoFile = "icon.png"
    credits = "GTCEu Modern Devs, Phoenixvine (PTF Dev), Herr Jolo (Chromatic Abberation Dev) & Witherschat (Universal Circuits Textures)"
    authors = "${mod_author}"
    description = "This mod provides additional content to enhance both packdev and the player experience in GTCEu Modern"

    [[dependencies.${mod_id}]]
        modId = "forge"
        mandatory = true
        versionRange = "[${forge_version},)"
        ordering = "NONE"
        side = "BOTH"
    [[dependencies.${mod_id}]]
        modId = "minecraft"
        mandatory = true
        versionRange = "[${minecraft_version},)"
        ordering = "NONE"
        side = "BOTH"
    [[dependencies.${mod_id}]]
        modId = "gtceu"
        mandatory = true
        versionRange = "[${gtceu_version},)"
        ordering = "AFTER"
        side = "BOTH"
    [[dependencies.${mod_id}]]
        modId = "jade"
        mandatory = false
        versionRange = "[${jade_version},)"
        ordering = "NONE"
        side = "BOTH"
